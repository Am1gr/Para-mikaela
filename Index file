<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
    <title>Para Mika 🤍</title>

    <!-- Fuentes de Google: Cormorant Garamond y Plus Jakarta Sans -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,600;0,700;1,400&family=Plus+Jakarta+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">

    <!-- Librería de Confeti -->
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>

    <style>
        :root {
            --primary: #d81b60;
            --primary-light: #f8bbd0;
            --rose-gold: #e0a96d;
            --bg-gradient: linear-gradient(135deg, #1a0b16 0%, #2a1226 50%, #150919 100%);
            --card-bg: rgba(255, 255, 255, 0.07);
            --text-main: #fcf8f9;
            --text-muted: #e1d5e0;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-tap-highlight-color: transparent;
        }

        html, body {
            width: 100%;
            height: 100%;
            overflow-x: hidden;
            font-family: 'Plus Jakarta Sans', sans-serif;
            background: var(--bg-gradient);
            color: var(--text-main);
            display: flex;
            justify-content: center;
            align-items: center;
        }

        #ambient-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
            pointer-events: none;
        }

        .wrapper {
            position: relative;
            z-index: 2;
            width: 100%;
            min-height: 100dvh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: clamp(1rem, 4vw, 2.5rem);
        }

        .card {
            position: relative;
            width: min(100%, 560px);
            background: var(--card-bg);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-radius: 28px;
            padding: clamp(1.8rem, 5vw, 3.2rem);
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5), inset 0 1px 1px rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.12);
            animation: cardAppear 1.2s cubic-bezier(0.16, 1, 0.3, 1) forwards;
            overflow: hidden;
        }

        .card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle at 30% 20%, rgba(216, 27, 96, 0.15) 0%, transparent 50%);
            pointer-events: none;
        }

        @keyframes cardAppear {
            from { opacity: 0; transform: translateY(30px) scale(0.96); }
            to { opacity: 1; transform: translateY(0) scale(1); }
        }

        .header-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: rgba(216, 27, 96, 0.15);
            border: 1px solid rgba(216, 27, 96, 0.3);
            padding: 6px 16px;
            border-radius: 50px;
            font-size: 0.85rem;
            color: #ff80ab;
            letter-spacing: 1px;
            text-transform: uppercase;
            font-weight: 600;
            margin-bottom: 1.2rem;
        }

        .header-badge svg {
            width: 14px;
            height: 14px;
            fill: currentColor;
        }

        h1 {
            font-family: 'Cormorant Garamond', serif;
            font-size: clamp(2.2rem, 6vw, 3.2rem);
            font-weight: 700;
            line-height: 1.15;
            background: linear-gradient(135deg, #ffffff 0%, #f8bbd0 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 0.8rem;
        }

        .sub-title {
            color: rgba(255, 255, 255, 0.7);
            font-size: clamp(0.95rem, 2.5vw, 1.1rem);
            font-weight: 300;
            margin-bottom: 2rem;
        }

        .divider {
            height: 1px;
            width: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            margin-bottom: 2rem;
        }

        .message-body {
            color: var(--text-muted);
            font-size: clamp(0.95rem, 2.2vw, 1.05rem);
            line-height: 1.8;
            font-weight: 300;
            text-align: left;
            margin-bottom: 2.2rem;
        }

        .message-body p { margin-bottom: 1.2rem; }
        .message-body p:last-child { margin-bottom: 0; }

        .highlight {
            color: #ffffff;
            font-weight: 500;
            border-bottom: 1px dotted rgba(255, 128, 171, 0.5);
        }

        .actions {
            display: flex;
            flex-direction: column;
            gap: 12px;
            align-items: center;
        }

        .btn-apology {
            width: 100%;
            max-width: 340px;
            padding: 16px 28px;
            background: linear-gradient(135deg, #e91e63 0%, #ad1457 100%);
            color: #ffffff;
            border: none;
            border-radius: 50px;
            font-family: 'Plus Jakarta Sans', sans-serif;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            box-shadow: 0 10px 25px rgba(233, 30, 99, 0.35);
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            outline: none;
        }

        .btn-apology:hover {
            transform: translateY(-3px) scale(1.02);
            box-shadow: 0 15px 30px rgba(233, 30, 99, 0.5);
        }

        .btn-apology:active { transform: translateY(1px) scale(0.98); }

        .response-card {
            display: none;
            margin-top: 1.5rem;
            padding: 1.2rem 1.5rem;
            background: rgba(46, 125, 50, 0.15);
            border: 1px solid rgba(129, 199, 132, 0.4);
            border-radius: 16px;
            color: #a5d6a7;
            font-size: 0.95rem;
            line-height: 1.6;
            animation: fadeIn 0.6s ease forwards;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .footer-note {
            margin-top: 2rem;
            font-size: 0.8rem;
            color: rgba(255, 255, 255, 0.4);
            letter-spacing: 0.5px;
        }

        @media (max-width: 480px) {
            .card { padding: 1.6rem 1.2rem; border-radius: 22px; }
            .header-badge { font-size: 0.75rem; padding: 4px 12px; }
        }
    </style>
</head>
<body>

    <canvas id="ambient-canvas"></canvas>

    <div class="wrapper">
        <div class="card">
            <div class="header-badge">
                <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
                Mensaje sincero
            </div>

            <h1>Mika, perdón de corazón</h1>
            <p class="sub-title">Para ti, porque te mereces toda mi sinceridad...</p>

            <div class="divider"></div>

            <div class="message-body">
                <p>Hola <span class="highlight">Mika</span>,</p>
                <p>Escribo esto porque no quería dejar pasar un momento más sin decirte sinceramente cuánto <span class="highlight">lamento haberte hecho daño</span>. Saber que mis acciones o palabras te causaron pesar me duele profundamente.</p>
                <p>Jamás fue mi intención lastimarte, pero entiendo perfectamente que eso no borra lo sucedido. Tu tranquilidad y tus sentimientos me importan demasiado, y me duele haber fallado en cuidarlos como debía.</p>
                <p>Valoro infinitamente quién eres y lo que significas para mí. No espero que todo cambie al instante, pero quiero que sepas que estoy dispuesto/a a aprender de esto y dar lo mejor de mí para remediarlo.</p>
            </div>

            <div class="actions">
                <button class="btn-apology" id="apologyBtn" onclick="aceptarDisculpas()">
                    <span>Acepto tus disculpas, hablemos</span> ✨
                </button>
            </div>

            <div id="responseCard" class="response-card">
                🤍 <strong>Gracias infinitas, Mika.</strong> Significa el mundo para mí que leas esto. Prometo cuidar mucho más de ti y de nuestra relación de aquí en adelante.
            </div>

            <div class="footer-note">Hecho con respeto y cariño especialmente para Mika</div>
        </div>
    </div>

    <script>
        const canvas = document.getElementById('ambient-canvas');
        const ctx = canvas.getContext('2d');
        let width = canvas.width = window.innerWidth;
        let height = canvas.height = window.innerHeight;

        window.addEventListener('resize', () => {
            width = canvas.width = window.innerWidth;
            height = canvas.height = window.innerHeight;
        });

        class Petal {
            constructor() { this.reset(); }
            reset() {
                this.x = Math.random() * width;
                this.y = -20;
                this.size = Math.random() * 8 + 6;
                this.speedY = Math.random() * 1.2 + 0.6;
                this.speedX = Math.random() * 0.8 - 0.4;
                this.opacity = Math.random() * 0.5 + 0.3;
                this.rotation = Math.random() * 360;
                this.spin = Math.random() * 2 - 1;
                this.color = ['#f8bbd0', '#ff80ab', '#ea80fc', '#ffffff'][Math.floor(Math.random() * 4)];
            }
            update() {
                this.y += this.speedY;
                this.x += Math.sin(this.y * 0.01) + this.speedX;
                this.rotation += this.spin;
                if (this.y > height + 20) this.reset();
            }
            draw() {
                ctx.save();
                ctx.translate(this.x, this.y);
                ctx.rotate((this.rotation * Math.PI) / 180);
                ctx.globalAlpha = this.opacity;
                ctx.fillStyle = this.color;
                ctx.beginPath();
                ctx.ellipse(0, 0, this.size, this.size / 2, 0, 0, Math.PI * 2);
                ctx.fill();
                ctx.restore();
            }
        }

        const petals = Array.from({ length: 35 }, () => new Petal());

        function animate() {
            ctx.clearRect(0, 0, width, height);
            petals.forEach(p => { p.update(); p.draw(); });
            requestAnimationFrame(animate);
        }
        animate();

        function aceptarDisculpas() {
            document.getElementById('responseCard').style.display = 'block';
            const btn = document.getElementById('apologyBtn');
            btn.style.transform = 'scale(0.95)';
            btn.style.opacity = '0.8';

            confetti({
                particleCount: 120,
                spread: 80,
                origin: { y: 0.65 },
                colors: ['#ff80ab', '#f8bbd0', '#e0a96d', '#ffffff']
            });
        }
    </script>
</body>
</html>
